"""
src/app.py — Application entry point.

This file is a thin shim that boots the FastAPI backend defined in `backend/`.
The old monolithic Flask app has been replaced by the modular FastAPI backend.

Run locally:
    python src/app.py
  or directly via uvicorn (recommended):
    uvicorn backend.app:app --host 0.0.0.0 --port 5000 --reload

API Docs (auto-generated by FastAPI):
    http://localhost:5000/docs
"""

import sys
import os

# Ensure the project root (parent of src/) is on sys.path so that
# `import backend` resolves correctly regardless of how this file is invoked.
PROJECT_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
if PROJECT_ROOT not in sys.path:
    sys.path.insert(0, PROJECT_ROOT)

import uvicorn

# Re-export for uvicorn discovery (e.g. `uvicorn src.app:app`)
from backend.app import app  # noqa: F401

if __name__ == "__main__":
    uvicorn.run(
        "backend.app:app",
        host="0.0.0.0",
        port=5000,
        reload=True,
    )
